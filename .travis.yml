language: python
python:
 - "3.9"
before_install: install:
 - pip install -r requirements.txt
 - pip install -U pytest
 - pip install codecov
install:
 - pip install .
env:
 global:
  - PYTHONPATH=src:test
script:
 - pytest
after_success:
 - codecov
